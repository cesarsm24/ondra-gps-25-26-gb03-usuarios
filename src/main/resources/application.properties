spring.profiles.active=dev

# ============================================
# CONFIGURACION GENERAL
# ============================================
spring.application.name=usuarios-service

# ============================================
# CONFIGURACION DEL SERVIDOR
# ============================================
server.port=${SERVER_PORT:8080}
server.servlet.context-path=/
server.servlet.encoding.enabled=true
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force=true

# Manejo de errores
server.error.include-message=always
server.error.include-binding-errors=always

# ============================================
# CONFIGURACION DE BASE DE DATOS (PostgreSQL)
# ============================================
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/Usuarios}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:12345}
spring.datasource.driver-class-name=org.postgresql.Driver

# ============================================
# CONFIGURACION DE JPA/HIBERNATE
# ============================================
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:create-drop}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ============================================
# GOOGLE OAUTH CONFIGURACION
# ============================================
google.oauth.client-id=${GOOGLE_OAUTH_CLIENT_ID:41556010027-4d8rs7q4ueggb72ql3v96maf9hn16cph.apps.googleusercontent.com}

# ============================================
# CONFIGURACION DE CIFRADO DE DATOS SENSIBLES
# ============================================
encryption.secret-key=${ENCRYPTION_SECRET_KEY:aB3dE6fG9hJ2kL5mN8pQ1rS4tU7vW0xY3zA6bC9dE2fG5hI8jK1lM4nO7pQ0rS3t}

# ============================================
# CONFIGURACION DEL SEEDER
# ============================================
seed.enabled=${SEED_ENABLED:true}

# ============================================
# CONFIGURACION JWT
# ============================================
jwt.secret=${JWT_SECRET:9ncK2ylOJN4Im5qPywB1+zA8voli6+2rLUTe0dAquqwB3YXAV8QLXmBFaJpLyl+ndUHwhA2BofXDI9x4NBLSVw==}
jwt.expiration=${JWT_EXPIRATION:900000}
jwt.refresh.expiration=${JWT_REFRESH_EXPIRATION:1728000000}

# ============================================
# CONFIGURACION DE EMAIL (GMAIL)
# ============================================
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:ondrasounds@gmail.com}
spring.mail.password=${MAIL_PASSWORD:wdanihtusokauywg}

# Propiedades SMTP
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# URL del frontend para enlaces en correos
app.frontend.url=${FRONTEND_URL:http://localhost:4200}

# ============================================
# CONFIGURACION DE CLOUDINARY
# ============================================
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dh6w4hrx7}
cloudinary.api-key=${CLOUDINARY_API_KEY:526714142285293}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:qLJlRntnLirFffII2l3IfgZb4bI}
cloudinary.folder=${CLOUDINARY_FOLDER:images}

# ============================================
# COMUNICACION CON OTROS MICROSERVICIOS
# ============================================
microservices.contenidos.url=${CONTENIDOS_SERVICE_URL:http://localhost:8081/api}
microservices.recomendaciones.url=${RECOMENDACIONES_SERVICE_URL:http://localhost:8082/api}
microservices.service-token=${SERVICE_TOKEN:e3e27aa2f289a9686be1d3c45659f308748be2d8751c4911b1cecb09fdd228fa}

# ============================================
# CONFIGURACION DE LOGGING
# ============================================
logging.level.root=${LOG_LEVEL_ROOT:INFO}
logging.level.com.ondra.users=${LOG_LEVEL_APP:DEBUG}
logging.level.org.springframework.web=${LOG_LEVEL_WEB:DEBUG}
logging.level.org.springframework.security=${LOG_LEVEL_SECURITY:DEBUG}
logging.level.org.hibernate.SQL=${LOG_LEVEL_SQL:DEBUG}
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=${LOG_LEVEL_SQL_BINDER:TRACE}

# ============================================
# CONFIGURACION DE ACTUATOR (Health Checks)
# ============================================
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoints.web.base-path=/actuator
management.endpoint.health.show-details=when-authorized
management.endpoint.health.show-components=when-authorized
management.health.db.enabled=true

management.info.env.enabled=true
management.info.java.enabled=true
management.info.os.enabled=true

# Información de la aplicación
info.app.name=ONDRA Usuarios Service
info.app.description=Microservicio de gestion de usuarios y artistas
info.app.version=1.0.0
info.app.encoding=UTF-8
info.app.java.version=17

# ============================================
# CORS Configuration
# ============================================
cors.allowed-origins=http://localhost:4200,http://localhost:3000
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
cors.max-age=3600

# ============================================
# API Base Paths Configuration
# ============================================
api.base.usuarios=/api/usuarios
api.base.artistas=/api/artistas
api.base.seguimientos=/api/seguimientos
api.base.config=/api/config
api.base.public=/api/public
api.base.internal=/api/internal
api.base.actuator=/actuator

# ============================================
# Security Configuration
# ============================================
security.role.service=SERVICE

# ============================================
# Security Endpoints Configuration
# ============================================

# Config Endpoints
security.endpoints.config.public=/public

# Usuarios Endpoints - Authentication & Registration
security.endpoints.usuarios.stats=/stats
security.endpoints.usuarios.login=/login
security.endpoints.usuarios.login-google=/login/google
security.endpoints.usuarios.refresh=/refresh
security.endpoints.usuarios.logout=/logout
security.endpoints.usuarios.logout-all=/logout-all

# Usuarios Endpoints - Password Recovery
security.endpoints.usuarios.recuperar-password=/recuperar-password
security.endpoints.usuarios.restablecer-password=/restablecer-password

# Usuarios Endpoints - Email Verification
security.endpoints.usuarios.verificar-email=/verificar-email
security.endpoints.usuarios.reenviar-verificacion=/reenviar-verificacion

# Usuarios Endpoints - Internal
security.endpoints.usuarios.datos-usuario=/*/datos-usuario
security.endpoints.usuarios.existe=/*/existe

# Usuarios Endpoints - Profile Management
security.endpoints.usuarios.onboarding=/onboarding-completado
security.endpoints.usuarios.cambiar-password=/cambiar-password
security.endpoints.usuarios.metodos-pago=/metodos-pago

# Artistas Endpoints
security.endpoints.artistas.redes=/redes
security.endpoints.artistas.metodos-cobro=/metodos-cobro
security.endpoints.artistas.renunciar=/renunciar

# Seguimientos Endpoints
security.endpoints.seguimientos.seguidos=/seguidos
security.endpoints.seguimientos.seguidores=/seguidores
security.endpoints.seguimientos.estadisticas=/estadisticas
security.endpoints.seguimientos.verificar=/verificar

# Actuator Endpoints
security.endpoints.actuator.health=/health

# ============================================
# Security Path Parameters Configuration
# ============================================
security.path.param.id=/{id}
security.path.param.id-usuario=/{idUsuario}
security.path.param.id-red=/{id_red}
security.path.param.wildcard=/**